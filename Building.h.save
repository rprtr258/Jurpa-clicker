#ifndef BUILDING_H
#define BUILDING_H

#include <sstream>
#include <string>

#include <SFML/Graphics.hpp>

class BuildingInfo;

class Building {
    public:
        Building();
        Building(int numberOfBuilding, int cost, int owned, int gain);
        Building(const Building &other);

        void setTexture(sf::Texture texture);
        void setWindow(const sf::RenderWindow &window);
        void setFont(const sf::Font &font);
        void setYshift(const int yshift);

        int getCost();
        void setCost(int cost);

        int getOwned();
        void setOwned(int owned);

        int getGain();
        void setGain(int
        std::string getFile();

        void setNumber(int number);
        void draw(sf::RenderWindow &window);
        void moveY(int yshift);
        int getX();

        static bool compareWithCost(const Building &first, const Building &second);
        static bool compareWithOwned(const Building &first, const Building &second);
        static bool compareWithGain(const Building &first, const Building &second);
    private:
        void setText(sf::Text &text,const char *str, int number);
        void setTextPosition(sf::Text &text, const sf::Font &font, int xshift, int yshift);

        int cost, owned, gain, numberOfBuilding, yshift;
        std::string textureFile;
        sf::Texture texture;
        sf::Sprite sprite;
        sf::Text costText,ownedText,gainText;
};
#include "BuildingsInfo.h"
#endif
